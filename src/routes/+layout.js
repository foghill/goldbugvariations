// Static promotional site — prerender every route to plain HTML so the
// SEO / schema.org markup ships server-rendered, matching the old deploy.
export const prerender = true;
export const ssr = true;
